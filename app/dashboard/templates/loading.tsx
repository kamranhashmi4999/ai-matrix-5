'use client';

const LoadingPage = () => {
    return (
        <div style={{ padding: 20, textAlign: 'center' }}>
            <h1>Loading...</h1>
        </div>
    );
};

export default LoadingPage;
